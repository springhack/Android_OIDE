版本 2.3.2
  *)修复gdb不能调试C/C++程序的问题
  *)在运行和编译前自动保存源码
 
版本 2.3.1
  *)编辑器与编译器分离，便于分别升级（将compiler.tar.gz置于SD卡根目录自动升级）
  *)某些Samsung平板/data分区造成的权限问题
  *)启动速度以及二进制文件的优化
  
版本 2.2
  *)修复交互界面问题
  *)对底层进程进行一些优化
  
版本 2.1
  *)修复了一些问题

版本 2.0
  *)对交互界面优化
  *)对资源优化
  *)更新一些环境变量

版本 1.1
  *)编译&运行按钮的添加
  *)更改了一些环境变量
  *)对布局些许优化
  *)对二次启动速度优化

版本 1.0
  *)修复编译器问题若干
  *)对布局做了一些优化
  *)tcc编译器降级至0.9.25（需要修复些许问题）
  *)更改一些环境变量
  *)可用的在线帮

版本 beta 1.1
  *)剔除root限制，无需安装编译器

版本 beta 1.0
  *)增加新图标，新欢迎界面
  *)剔除无用功能，增加在线帮助
  *)优化启动速度，稳定性
